# Global Rules

  

## Working Rules

  

**Think before coding.** State your assumptions out loud. If the request is ambiguous, ask. If a simpler approach exists, push back. Stop when you are confused, name what is unclear, do not just pick one interpretation and run. When you have to guess at any part of an answer, flag the guess explicitly, explain why you cannot give a definitive answer, and say how you arrived at it. Never fill in missing information silently.

  

**Direct and honest.** Act as a ruthless mentor: say when something is wrong, lead with the better approach, and do not soften feedback to the point of obscuring the message. Focus on what is actually correct, not what is easy to hear.

  

**Simplicity first.** Write the minimum code that solves the problem. No speculative abstractions. No flexibility nobody asked for. The test: would a senior engineer call this overcomplicated.

  

**Surgical changes.** Touch only what the task requires. Do not improve neighboring code. Do not refactor what is not broken. Every changed line should trace back to the request.

  

**Goal-driven execution.** Turn vague instructions into verifiable targets before writing a line. "Add validation" becomes "write tests for invalid inputs, then make them pass."

  

**No em dashes.** Never use em dashes (--) or (---) in any output unless explicitly asked.

  

## Snowflake and dbt Development Preferences

  

### Tooling

  

- Always use `/Users/dworoniecki/.local/bin/dbt` (aliased as `dbtf` in zsh). This is dbt-fusion. Never use the system `dbt` binary. The shell alias is not available in non-interactive bash, so always use the full path.

- Run `dbt compile` often to inspect the raw SQL generated by Jinja. It is the most effective way to debug syntax errors and unexpected whitespace.

  

### Snowflake SQL

  

- Use `IFF` for simple single-condition logic and `CASE` when the logic is more involved (multiple conditions, nested logic, or when clarity benefits from explicit WHEN/THEN/ELSE structure).

- Use `ZEROIFNULL` and `NULLIFZERO` instead of `COALESCE` when the intent is specifically to convert between null and zero.

  

### Jinja: When and How to Use It

  

- Use Jinja where it adds real value: `ref`/`source`, config blocks, macros for repeated logic, conditional logic for environment differences. Prefer plain SQL when it is already clear and readable.

- Favor readability over DRY-ness. Avoid turning every repeated line into a macro. Overusing Jinja makes models harder to onboard and harder to review in pull requests.

- Treat macros like functions: document their arguments in schema files and break complex logic into smaller, reusable pieces.

  

### Jinja Syntax Rules

  

- Use spaces inside delimiters: `{{ this }}` not `{{this}}`.

- Never nest curly brackets inside a Jinja expression. Once inside `{{ ... }}`, do not open another `{{ }}` for nested calls.

- Correct: `{{ date_spine(start_date=var('start_date')) }}`

- Incorrect: `{{ date_spine(start_date={{ var('start_date') }}) }}`

- Define variables with `{% set %}` at the top of model or macro files so they can be referenced in multiple places and are easy to scan.

- Use whitespace control (`{%-`, `{{-`) sparingly. Readability of the source code matters more than perfection of the compiled output.

- Separate logical Jinja blocks with newlines. Indent code inside a Jinja block by 4 spaces to visually signal it is wrapped.

  

### Jinja Execution Model

  

- Jinja runs at compile time, not when the database executes the query. It is for generating SQL structure, not for performing runtime calculations on table data.

- For logic that requires results from one query to build another, use the `execute` variable to ensure the code only runs during the execution phase.

  

## Python Best Practices

  

### Logging

- Always use the `logging` module. Never use `print()` for runtime output in scripts or pipelines.

- Configure logging at the module level using `logger = logging.getLogger(__name__)`.

- Use appropriate log levels: DEBUG for dev-only detail, INFO for normal execution milestones, WARNING for recoverable issues, ERROR for caught exceptions, CRITICAL for fatal failures.

- For scripts run via CLI, configure a root logger with format: `%(asctime)s | %(levelname)s | %(name)s | %(message)s`.

- Never configure logging inside library functions. Only configure at the entry point (main block).

  

### Error Handling

- Always handle exceptions explicitly. Never use bare `except:` clauses.

- Catch the most specific exception type possible. Use `except Exception as e` only as a last resort and always log `e` with context.

- Always log the exception before re-raising or failing: `logger.error("Context about what failed: %s", e, exc_info=True)`.

- Use `exc_info=True` on error logs to capture the full traceback.

- Do not silently swallow exceptions. If you catch it and cannot recover, re-raise or raise a more descriptive custom exception.

  

### Structure and Modularity

- Prefer functions over classes. Only use a class when you genuinely need to manage state across method calls or are implementing a well-defined interface. Utility logic, transformations, and pipeline steps should be plain functions.

- A collection of related functions in a module is not a reason to wrap them in a class. Use a module for grouping, not a class.

- Functions should do one thing. If a function needs more than one paragraph of explanation, split it.

- Avoid deeply nested logic. Use early returns to reduce nesting.

- All scripts must have a `if __name__ == "__main__":` guard.

- Use type hints on all function signatures.

  

### Naming and Readability

- Use descriptive names. A variable named `df` is acceptable for a short-lived intermediate; anything passed between functions should have a name that describes its content (e.g. `loan_balances_df`).

- Boolean variables and functions that return booleans should read as statements: `is_valid`, `has_missing_keys`, `was_processed`.

- Constants should be `UPPER_SNAKE_CASE` and defined at the top of the module, not inline.

  

### Function Design

- Functions should have no side effects unless that is their explicit purpose. A function that transforms data should return the result, not mutate the input in place.

- Avoid functions with more than 3-4 positional arguments. Use keyword arguments or a dataclass/TypedDict for parameter grouping when the signature gets unwieldy.

- Default argument values should never be mutable objects (no `def f(x, data=[])`).

  

### Dependencies and Environment

- All dependencies must be declared explicitly (requirements.txt or pyproject.toml). No implicit environment assumptions.

- Never hardcode credentials, paths, or environment-specific values. Use environment variables or config files.

  

### Pandas / Data Processing

- Avoid chained assignments. Use `.loc[]` for all in-place modifications.

- Be explicit about dtypes on load where the schema is known.

- Validate dataframe shape and key columns after any major transformation step (log the result, do not assert silently).